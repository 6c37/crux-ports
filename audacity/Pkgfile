# Description: A program that lets you manipulate digital audio waveforms.
# URL:         https://github.com/audacity/audacity
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports/issues
# Depends on:  ffmpeg cmake python lame libmad wxgtk 
# Optional:    vamp-plugin-sdk libid3tag

name=audacity
version=2.2.1
release=2
source=(
	https://github.com/$name/$name/archive/Audacity-$version.tar.gz
	Fix-building-without-libid3tag-230.patch
)

build() {
	cd $name-Audacity-$version

	patch -p1 < $SRC/Fix-building-without-libid3tag-230.patch

	CONFIG_SHELL=/bin/bash ./configure --prefix=/usr

	make
	make DESTDIR=$PKG install

	rm -rf $PKG/usr/share/{locale,doc}
}
