# Description: 	A FUSE filesystem similar to web servers mod_rewrite. It can change the name of accessed files on-the-fly. - git checkout
# URL:		https://github.com/sloonz/rewritefs
# Packager:	Samuel Hand, samuel dot hand at openmailbox dot org
# Maintainer:	Samuel Hand, samuel dot hand at openmailbox dot org
# Depends on:	fuse libpcre

name=rewritefs
version=git
release=1
source=()

build(){
	cd $PKGMK_SOURCE_DIR

	if [[ -d $name ]]; then
		cd $name
		git pull git://github.com/sloonz/$name
	else
		git clone git://github.com/sloonz/rewritefs $name
		cd $name
	fi
	make DESTDIR=$PKG install
}
