# Description: The slang library version 2.
# URL:         http://www.jedsoft.org/slang/
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports/issues
# Depends on:  libpng

name=libslang2
version=2.3.2
release=1
source=(http://www.jedsoft.org/releases/${name:3:5}/${name:3:5}-$version.tar.bz2)

build () {
	cd ${name:3:5}-$version

	./configure \
		--prefix=/usr \
		--sysconfdir=/etc

	make -j1 all
	make -j1 DESTDIR=$PKG install-all

	rm -rf $PKG/usr/share/doc
}
