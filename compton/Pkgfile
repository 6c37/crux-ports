# Description: A compositor for X - git checkout
# URL:         https://github.com/chjj/compton
# Packager:    Luiz de Milon, kori at openmailbox dot org
# Maintainer:  Luiz de Milon, kori at openmailbox dot org
# Depends on:  libconfig libpcre mesa3d xorg-libxcomposite xorg-libxinerama xorg-libxrandr xorg-libxdamage

name=compton
version=git
release=1
source=()

build() {
	cd $PKGMK_SOURCE_DIR
	
	if cd $name; then
		git pull git://github.com/chjj/compton.git
	else
		git clone git://github.com/chjj/compton.git $name
		cd $name
	fi

	make NO_DBUS=1 compton
	install -Dm755 compton $PKG/usr/bin/compton
}
